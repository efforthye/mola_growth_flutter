import 'package:flutter/material.dart';

class GameContent extends StatelessWidget {
  const GameContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('게임 콘텐츠가 여기에 들어갑니다'),
    );
  }
}